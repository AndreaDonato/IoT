#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

srand(time(NULL));

// Funzione Aggiornamento (Azione tra stati CdM)
void take_action()
{

}

// Random Extractor
int random(int max)
{
	return rand();
}

// Calcola Delta
int delta(int *chain)
{
	return rand();	
}